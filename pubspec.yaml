name: recipes
description: A recipes app using cooklang
publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 0.0.1+1

environment:
  sdk: ">=2.19.5 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  dynamic_color: ^1.6.3
  flutter_bloc: ^8.1.2
  equatable: ^2.0.5
  cooklang: ^1.0.2
  universal_platform: ^1.0.0+1
  path_provider: ^2.0.15
  logger: ^1.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
